#ifndef FIRST_H_
#define FIRST_H_

// return 1 if yes,
// return 0 otherwise
int first_stat(char* tkid);
int first_mvars(char* tkid);
int first_expr(char* tkid);
int first_xhelp(char* tkid);
int first_M(char* tkid);
int first_R(char* tkid);


#endif