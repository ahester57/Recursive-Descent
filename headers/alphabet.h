#ifndef ALPHABET_H_
#define ALPHABET_H_

int isinalphabet(const char c);
int isletter(const char c);
int isnumber(const char c);
int isoperator(const char c);
int isdelim(const char c);
int iswhitespace(const char c);
int isendofline(const char c);

#endif